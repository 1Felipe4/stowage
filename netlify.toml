[build]
  base = "web"
  command = "npm install --prefix .. && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_SERVER_URL = "https://stowage-iua0.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
